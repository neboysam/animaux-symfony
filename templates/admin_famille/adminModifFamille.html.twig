{% extends 'base.html.twig' %}

{% block title %}Gestion des familles{% endblock %}

{% block monTitre %}Gestion de famille {{famille.libelle}}{% endblock %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-6">
                {{form_start(form)}}
                    {{form_row(form.libelle)}}
                    {{form_row(form.description)}}
                    {{form_row(form.animaux)}}
                    <input type="submit" class="btn btn-primary" value="Enregistrer">
                {{form_end(form)}}
            </div>
        </div>
    </div>
    {# {{dump(animal)}} #}
{% endblock %}
